"""Pricing core: Strategy + Protocol + Registry.

Reemplaza la escalera `if _is_*_type()` de `services.py` por una tabla de
estrategias (una por familia de bono). La fachada `FinancialEngine` delega acá.
"""
