# Monitor de Bonos · Balanz — dependencias de runtime.
# Python 3.12 del sistema, SIN venv (límite OneDrive). En esta máquina el
# runtime es el "Microsoft Store" Python 3.12, no el de Programs — instalar con:
#   & "$env:LOCALAPPDATA\Microsoft\WindowsApps\python3.12.exe" -m pip install -r requirements.txt

# --- núcleo financiero (sin cambios) ---
numpy
scipy
pandas
python-dateutil
holidays
pandas-market-calendars
openpyxl                 # solo seeding (scripts/ingest_master.py)
websockets               # futures_provider

# --- nuevas (reingeniería) ---
fastapi
uvicorn[standard]
httpx
jinja2
python-multipart
sse-starlette
duckdb
SQLAlchemy>=2
pydantic>=2
pydantic-settings

# --- dev ---
pytest
