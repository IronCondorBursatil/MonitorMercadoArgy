# Monitor de Bonos · Balanz — dependencias de runtime.
# Python 3.12 del sistema, SIN venv (límite OneDrive). El runtime es el Python de
# Programs (py -3.12). El antiguo "Store Python" ya no existe. Instalar con:
#   py -3.12 -m pip install -r requirements.txt
#   (equiv.: & "$env:LOCALAPPDATA\Programs\Python\Python312\python.exe" -m pip install -r requirements.txt)

# --- núcleo financiero (sin cambios) ---
numpy
scipy
pandas
python-dateutil
holidays
pandas-market-calendars
openpyxl                 # solo seeding (scripts/ingest_master.py)
websockets               # futures_provider

# --- nuevas (reingeniería) ---
fastapi
uvicorn[standard]
httpx
jinja2
python-multipart
sse-starlette
SQLAlchemy>=2
pydantic>=2
pydantic-settings
optionlab                # analytics avanzados de estrategias de opciones (P(profit), EV, ranges)

# --- dev ---
pytest
hypothesis              # property-based testing (invariantes de pricing/day-count)
ruff                    # linter (config en pyproject.toml): F/E/W, sin reformateo masivo
