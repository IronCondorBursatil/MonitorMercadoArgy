# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.hypothesis/          # base de ejemplos de property-based testing (regenerable)

# Logs
*.log
# Rotados de logrotate/systemd: `*.log` NO los matchea (terminan en .1, .2, ...).
# Sin esto quedan como untracked en la raiz y ensucian el arbol en cada git pull.
*.log.[0-9]
*.log.[0-9].gz

# Generated artifacts (PNG outputs, caches)
data/monitor_*.png
data/feriados_ar_cache.json
apps/cli/*.png
apps/cli/*.json
# PDF del panel ON: lo genera scripts/export_on_pdf.py en la raíz, es salida derivada
# (y pesa; la raíz está sincronizada por OneDrive).
Obligaciones_Negociables_*.pdf

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/

# Backups manuales de Excel / data
*.bak
*.bak[0-9]

# Material de referencia BYMA (addin Excel, PDFs, scripts de auditoría) — 9.6 MB,
# fuente del pipeline que genera data/byma/titulos_final.csv; no se versiona.
BYMADATA_EXCEL/

# Artefactos de Playwright MCP (snapshots/console de pruebas de UI)
.playwright-mcp/

# Scratch
scratch/

# Bases de datos (reingeniería) — viven en %LOCALAPPDATA%\monitor, nunca en el repo.
*.db
*.duckdb
*.db-wal
*.db-shm
monitor/

# Baseline de regresión (artefacto local de Fase 0)
baseline_tests.txt

# Datos de runtime del usuario (tenencias de la cartera)
data/cartera.json

# Secrets / credentials (NEVER commit)
.env
.env.*
!.env.example
secrets/
*.secret

# Claude Code: estado local (settings.local, etc.) — no se trackea
.claude/

# Worktrees de Superpowers: NUNCA project-local (OneDrive). Ver "Flujo Superpowers"
# en CLAUDE.md — usar la tool nativa o el path global ~/.config/superpowers/worktrees/.
.worktrees/
worktrees/

